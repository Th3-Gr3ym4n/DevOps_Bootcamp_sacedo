pipeline {
    agent {
        docker {
            image ''
        }
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Th3-Gr3ym4n/DevOps_Bootcamp_sacedo.git'
            }
        }

        stage1('Verify') {
            steps {
                sh ''' 
                ls -lah
                '''
                sh 'printenv | sort'

            }
        }

    }
}

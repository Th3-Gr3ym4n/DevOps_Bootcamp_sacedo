pipeline {
    agent any

    stages {
        stage('Verify') {
            steps {
                sh ''' 
                ls -lah
                '''
                sh 'printenv | sort'

            }
        }

    }
}
